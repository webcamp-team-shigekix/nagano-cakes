<div class="container">
    <div class="row my-5">
        <div class="mainVisual col-md-8 offset-md-2 py-5 border">
            <div class="mainVisual-text col-md-8 offset-md-2 border p-3 my-5">
                <p>
                    ようこそ、ながのCAKEへ<br>
                    このサイトは、ケーキ販売のECサイトになります。<br>
                    会員でない方も商品の閲覧はできますが、<br>
                    購入には会員登録が必要になります。
                </p>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            <h3 class="text-left">新着商品</h3>
        </div>
    </div>
    <div class="row d-flex justify-content-center my-3">
        <% @products.reverse.each do |product| %>
            <div class="col-md-3 my-2">
                <%= link_to product_path(product.id), class: "text-decoration-none" do %>
                    <div class="card">
                        <%= attachment_image_tag(product, :image, :fill, 200, 200, fallback: "no_image.jpg") %>
                        <div class="card-body text-dark">
                            <div class="card-text"><%= product.name %></div>
                            <div class="card-text">¥ <%= converting_to_jpy(product.price) %></div>
                        </div>
                    </div>
                <% end %>
            </div>
        <% end %>
        <div class="col-md-12 d-flex justify-content-end mt-3">
           <p class="text-right"><%= link_to "全ての商品を見る>", products_path, class: "btn btn-outline-secondary" %></p>
        </div>
    </div>
    <div class="row mb-5">
        <div class="col-md-12">
            <h3 class="text-left">ジャンル検索</h3>
            <p class="text-center">検索したいジャンルを入力してください。</p>
        </div>
        <div class="col-md-12 d-flex justify-content-center">
            <%= form_with url: customers_products_search_type_path , method: :get, local: true do |f|%>
                <div class="col-md-12 d-flex">
                    <div><%= f.collection_select :type, ProductType.all, :id, :name, { prompt: "すべて" }, class: "form-control" %></div>
                    <div><%= f.submit '検索', class: "btn btn-outline-secondary"%></div>
                </div>
            <% end %>
        </div>
    </div>
</div>
