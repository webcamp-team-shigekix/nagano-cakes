<div class="container my-5">
  <div class="row">
    <div class="offset-md-1 col-md-10">
      <h3>ログイン</h3>
      <h5>会員の方はこちらからログイン</h5>
    </div>
  </div>
  <div class="row">
    <div class="offset-md-3 col-md-6">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="my-3 d-md-flex">
          <div class="col-md-4"><%= f.label :email, "メールアドレス" %></div>
          <div class="col-md-8"><%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %></div>
        </div>
        <div class="my-3 d-md-flex">
          <div class="col-md-4"><%= f.label :password, "パスワード" %></div>
          <div class="col-md-8"><%= f.password_field :password, autocomplete: "current-password", class: "form-control" %></div>
        </div>
        <% if devise_mapping.rememberable? %>
          <div class="text-center my-3">
            <%= f.check_box :remember_me %>
            <%= f.label :remember_me %>
          </div>
        <% end %>
        <div class="col-md-12 d-flex justify-content-center my-3"><%= f.submit "ログイン", class:"btn btn-primary" %></div>
      <% end %>
    </div>
  </div>
  <div class="row mt-3">
    <div class="offset-md-2 col-md-5 p-2">
      会員登録がお済みでない方は<br>
      <%= link_to "こちら", new_customer_registration_path %> から新規登録を行ってください
    </div>
    <div class="col-md-5 p-2">
      パスワードを忘れた方は<br>
      <%= link_to "こちら", new_customer_password_path %> から変更手続きを行ってしてください
    </div>
  </div>
</div>
