<div class="container mt-4">
  <h2>ログイン</h2>
  <div class="row mt-4"><h4>会員の方はこちらからログイン</h4></div>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <%= flash[:error] %>
    <div class="field row mt-4">
      <div class="col-md-3"><%= f.label :email, "メールアドレス" %></div>
      <div class="col-md-3"><%= f.email_field :email, autofocus: true, autocomplete: "email" %></div>
    </div>

    <div class="field row mt-2">
      <div class="col-md-3"><%= f.label :password, "パスワード" %></div>
      <div class="col-md-3"><%= f.password_field :password, autocomplete: "current-password" %></div>
    </div>

    <% if devise_mapping.rememberable? %>
      <div class="field mt-2">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me %>
      </div>
    <% end %>

    <div class="actions row mt-4 ">
      <div class="col-md-3"></div>
      <div class="col-md-3"><%= f.submit "ログイン", class:"btn btn-primary w-50" %></div>
    </div>
  <% end %>

  <div class="row mt-5">
    <div class="col-md-4"><p>会員登録がお済みでない方は</p></div>
    <div class="col-md-4"><p>パスワードを忘れた方は</p></div>
  </div>
  <div class="row">
    <div class="col-md-4  mb-3"><%= link_to "こちら", new_customer_registration_path %> から新規登録を行ってください</div>
    <div class="col-md-4  mb-3"><%= link_to "こちら", new_customer_password_path %> から変更手続きを行ってしてください</div>
  </div>
</div>
